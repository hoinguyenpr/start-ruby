def if_statement
    x = 10
    if x > 2
        puts "if: x is greater than 2"
    elsif x <= 2 and x!=0
        puts "if: x is 1"
    else
        puts "if: I can't guess the number"
    end
end

if_statement